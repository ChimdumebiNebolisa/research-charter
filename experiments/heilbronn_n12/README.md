# Heilbronn experiment records

Foundation Phase contains no research experiments. After explicit Phase 2 authorization, store append-oriented `experiment` and `candidate` JSON records here and preserve failed runs and raw artifacts.
